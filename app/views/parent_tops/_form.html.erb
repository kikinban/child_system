<%= form_with(model: parent_top) do |form| %>
  <% if parent_top.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(parent_top.errors.count, "error") %> prohibited this parent_top from being saved:</h2>

      <ul>
        <% parent_top.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
